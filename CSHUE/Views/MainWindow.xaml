<Window x:Class="CSHUE.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CSHUE"
        Height="450"
        Width="650"
        WindowStyle="None"
        BorderThickness="1"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:fw="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
        BorderBrush="{Binding Path=(fw:AccentColors.ImmersiveSystemAccentBrush)}"
        MinWidth="722"
        MinHeight="322"
        Name="Window"
        SourceInitialized="Window_SourceInitialized"
        Activated="Window_Activated"
        Deactivated="Window_Deactivated"
        fw:AcrylicWindow.Enabled="True"
        fw:AcrylicWindow.ShowTitleBar="False"
        FontSize="14"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="Transparent"
        fw:PointerTracker.Enabled="False"
        d:DesignHeight="450" d:DesignWidth="800"
        Foreground="{DynamicResource SystemBaseHighColorBrush}"
        Closing="Window_Closing"
        FontFamily="Segoe UI Semilight">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <materialDesign:PackIconKind x:Key="MaximizeRestoreIcon">CropSquare</materialDesign:PackIconKind>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Name="Menus" Background="{DynamicResource SystemAltMediumColorBrush}">
            <StackPanel>
                <Grid Height="32">
                    <TextBlock Text="   CSHUE" VerticalAlignment="Center" FontSize="15" />
                </Grid>
                
                <Grid Height="16" />
                
                <Grid Name="Home"
                      Height="48"
                      MouseLeftButtonDown="Control_MouseLeftButtonDown"
                      MouseLeftButtonUp="Control_MouseLeftButtonUp"
                      Style="{DynamicResource Windows10Menu}">
                    <StackPanel Orientation="Horizontal">
                        <Grid Name="HomeIndicator" HorizontalAlignment="Left" Height="24" Width="4" Background="{Binding Path=(fw:AccentColors.ImmersiveSystemAccentBrush)}" />
                        <materialDesign:PackIcon Kind="HomeOutline" Margin="12" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon.LayoutTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </materialDesign:PackIcon.LayoutTransform>
                        </materialDesign:PackIcon>
                        <TextBlock Text="{Binding Path=Home, Source={StaticResource Resources}}" Margin="2,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>

                <Grid Name="Config"
                      Height="48"
                      MouseLeftButtonDown="Control_MouseLeftButtonDown"
                      MouseLeftButtonUp="Control_MouseLeftButtonUp"
                      Style="{DynamicResource Windows10Menu}">
                    <StackPanel Orientation="Horizontal">
                        <Grid Name="ConfigIndicator" HorizontalAlignment="Left" Height="24" Width="4" Background="{Binding Path=(fw:AccentColors.ImmersiveSystemAccentBrush)}" Visibility="Hidden" />
                        <materialDesign:PackIcon Kind="WrenchOutline" Margin="12" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon.LayoutTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </materialDesign:PackIcon.LayoutTransform>
                        </materialDesign:PackIcon>
                        <TextBlock Text="{Binding Path=CSGOGSI, Source={StaticResource Resources}}" Margin="2,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <materialDesign:PackIcon Kind="AlertCircleOutline"
                                             Margin="12"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Right"
                                             Foreground="Orange"
                                             Visibility="{Binding WarningCsgoVisibility}"
                                             ToolTip="{Binding Path=WarningCSGO, Source={StaticResource Resources}}"
                                             ToolTipService.InitialShowDelay="100"
                                             ToolTipService.HorizontalOffset="-70"
                                             Background="Transparent">
                        <materialDesign:PackIcon.LayoutTransform>
                            <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                        </materialDesign:PackIcon.LayoutTransform>
                    </materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="AlertCircleOutline"
                                             Margin="12"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Right"
                                             Foreground="Orange"
                                             Visibility="{Binding WarningGsiVisibility}"
                                             ToolTip="{Binding Path=WarningGSI, Source={StaticResource Resources}}"
                                             ToolTipService.InitialShowDelay="100"
                                             ToolTipService.HorizontalOffset="-70"
                                             Background="Transparent">
                        <materialDesign:PackIcon.LayoutTransform>
                            <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                        </materialDesign:PackIcon.LayoutTransform>
                    </materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="AlertCircleOutline"
                                             Margin="12"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Right"
                                             Foreground="Orange"
                                             Visibility="{Binding WarningGsiCorruptedVisibility}"
                                             ToolTip="{Binding Path=WarningGSICorrupted, Source={StaticResource Resources}}"
                                             ToolTipService.InitialShowDelay="100"
                                             ToolTipService.HorizontalOffset="-70"
                                             Background="Transparent">
                        <materialDesign:PackIcon.LayoutTransform>
                            <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                        </materialDesign:PackIcon.LayoutTransform>
                    </materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="AlertCircleOutline"
                                             Margin="12"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Right"
                                             Foreground="Orange"
                                             Visibility="{Binding WarningSteamVisibility}"
                                             ToolTip="{Binding Path=WarningSteam, Source={StaticResource Resources}}"
                                             ToolTipService.InitialShowDelay="100"
                                             ToolTipService.HorizontalOffset="-70"
                                             Background="Transparent">
                        <materialDesign:PackIcon.LayoutTransform>
                            <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                        </materialDesign:PackIcon.LayoutTransform>
                    </materialDesign:PackIcon>
                </Grid>
            </StackPanel>

            <StackPanel VerticalAlignment="Bottom">
                <Grid Name="Donate"
                      Height="48"
                      MouseLeftButtonDown="Control_MouseLeftButtonDown"
                      MouseLeftButtonUp="Control_MouseLeftButtonUp"
                      Style="{DynamicResource Windows10Menu}">
                    <StackPanel Orientation="Horizontal">
                        <Grid Name="DonateIndicator" HorizontalAlignment="Left" Height="24" Width="4" Background="{Binding Path=(fw:AccentColors.ImmersiveSystemAccentBrush)}" Visibility="Hidden" />
                        <materialDesign:PackIcon Foreground="Gray" Kind="CoffeeOutline" Margin="12" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon.LayoutTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </materialDesign:PackIcon.LayoutTransform>
                        </materialDesign:PackIcon>
                        <TextBlock Foreground="Gray" Text="{Binding Path=Donate, Source={StaticResource Resources}}" Margin="2,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Grid Name="About"
                      Height="48"
                      MouseLeftButtonDown="Control_MouseLeftButtonDown"
                      MouseLeftButtonUp="Control_MouseLeftButtonUp"
                      Style="{DynamicResource Windows10Menu}">
                    <StackPanel Orientation="Horizontal">
                        <Grid Name="AboutIndicator" HorizontalAlignment="Left" Height="24" Width="4" Background="{Binding Path=(fw:AccentColors.ImmersiveSystemAccentBrush)}" Visibility="Hidden"/>
                        <materialDesign:PackIcon Foreground="Gray" Kind="AboutOutline" Margin="12" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon.LayoutTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </materialDesign:PackIcon.LayoutTransform>
                        </materialDesign:PackIcon>
                        <TextBlock Foreground="Gray" Text="{Binding Path=About, Source={StaticResource Resources}}" Margin="2,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Grid Name="Settings"
                      Height="48"
                      MouseLeftButtonDown="Control_MouseLeftButtonDown"
                      MouseLeftButtonUp="Control_MouseLeftButtonUp"
                      Style="{DynamicResource Windows10Menu}">
                    <StackPanel Orientation="Horizontal">
                        <Grid Name="SettingsIndicator" HorizontalAlignment="Left" Height="24" Width="4" Background="{Binding Path=(fw:AccentColors.ImmersiveSystemAccentBrush)}" Visibility="Hidden"/>
                        <materialDesign:PackIcon Kind="SettingsOutline" Margin="12" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon.LayoutTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </materialDesign:PackIcon.LayoutTransform>
                        </materialDesign:PackIcon>
                        <TextBlock Text="{Binding Path=Settings, Source={StaticResource Resources}}" Margin="2,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>

        <Grid Background="{DynamicResource SystemAltHighColorBrush}" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="46" />
                    <ColumnDefinition Width="46" />
                    <ColumnDefinition Width="46" />
                </Grid.ColumnDefinitions>

                <Grid WindowChrome.IsHitTestVisibleInChrome="True" Name="MinimizeButton" Height="32"
                    MouseLeftButtonDown="TopControls_MouseLeftButtonDown"
                    MouseLeftButtonUp="TopControls_MouseLeftButtonUp"
                    Grid.Column="1"
                    VerticalAlignment="Top"
                    Background="Transparent">
                    <materialDesign:PackIcon Kind="WindowMinimize" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

                <Grid WindowChrome.IsHitTestVisibleInChrome="True" Name="MaximizeButton" Height="32"
                    MouseLeftButtonDown="TopControls_MouseLeftButtonDown"
                    MouseLeftButtonUp="TopControls_MouseLeftButtonUp"
                    Grid.Column="2"
                    VerticalAlignment="Top"
                    Background="Transparent">
                    <materialDesign:PackIcon Kind="{DynamicResource MaximizeRestoreIcon}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

                <Grid WindowChrome.IsHitTestVisibleInChrome="True" Name="CloseButton" Height="32"
                    MouseLeftButtonDown="TopControls_MouseLeftButtonDown"
                    MouseLeftButtonUp="TopControls_MouseLeftButtonUp"
                    Grid.Column="3"
                    VerticalAlignment="Top"
                    Background="Transparent">
                    <materialDesign:PackIcon Kind="WindowClose" VerticalAlignment="Center" HorizontalAlignment="Center" />
                </Grid>
            </Grid>

            <Frame Name="Page" Grid.Row="1" NavigationUIVisibility="Hidden"/>

            <!--<Image Grid.Row="1"
                   Width="128"
                   Height="128"
                   gif:AnimationBehavior.SourceUri="/Images/Loading-Dark.gif"
                   gif:AnimationBehavior.RepeatBehavior="Forever"/>-->

            <Canvas Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <Path Stroke="White"
                      StrokeThickness="7">
                    <Path.Data>
                        <PathGeometry>
                            <PathFigure StartPoint="0 -100">
                                <ArcSegment x:Name="Arc"
                                            Size="100 100"
                                            SweepDirection="ClockWise" />
                            </PathFigure>
                        </PathGeometry>
                    </Path.Data>

                    <Path.RenderTransform>
                        <RotateTransform x:Name="Rotate" />
                    </Path.RenderTransform>

                    <Path.Triggers>
                        <EventTrigger RoutedEvent="Path.Loaded">
                            <BeginStoryboard>
                                <Storyboard TargetName="Arc"
                                            AutoReverse="True"
                                            RepeatBehavior="Forever">
                                    <PointAnimationUsingPath
                                        Storyboard.TargetProperty="Point"
                                        Duration="0:0:9"
                                        AccelerationRatio=".5"
                                        DecelerationRatio=".5">
                                        <PointAnimationUsingPath.PathGeometry>
                                            <PathGeometry
                                                Figures="M 8.715574274 -99.619469809 A 100 100 0 1 1 -100 0" />
                                        </PointAnimationUsingPath.PathGeometry>
                                    </PointAnimationUsingPath>

                                    <BooleanAnimationUsingKeyFrames
                                        Storyboard.TargetProperty="IsLargeArc"
                                        Duration="0:0:9"
                                        AccelerationRatio=".5"
                                        DecelerationRatio=".5">
                                        <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="False" />
                                        <DiscreteBooleanKeyFrame KeyTime="0:0:5.95" Value="True" />
                                    </BooleanAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                            <BeginStoryboard>
                                <Storyboard TargetName="Rotate"
                                            RepeatBehavior="Forever">
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Angle"
                                        From="0"
                                        To="300"
                                        Duration="0:0:9" />
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Angle"
                                        From="300"
                                        To="600"
                                        AccelerationRatio=".5"
                                        DecelerationRatio=".5"
                                        BeginTime="0:0:9"
                                        Duration="0:0:9" />
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Angle"
                                        From="600"
                                        To="900"
                                        BeginTime="0:0:18"
                                        Duration="0:0:9" />
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Angle"
                                        From="900"
                                        To="1200"
                                        AccelerationRatio=".5"
                                        DecelerationRatio=".5"
                                        BeginTime="0:0:27"
                                        Duration="0:0:9" />
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Angle"
                                        From="1200"
                                        To="1500"
                                        BeginTime="0:0:36"
                                        Duration="0:0:9" />
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Angle"
                                        From="1500"
                                        To="1800"
                                        AccelerationRatio=".5"
                                        DecelerationRatio=".5"
                                        BeginTime="0:0:45"
                                        Duration="0:0:9" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Path.Triggers>
                </Path>
            </Canvas>
        </Grid>
    </Grid>
</Window>
